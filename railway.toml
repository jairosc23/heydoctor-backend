# Railway – HeyDoctor Backend (jairosc23/heydoctor-backend)
# Root Directory en Dashboard: vacío (este repo es solo backend en la raíz)

[build]
builder = "nixpacks"

[deploy]
startCommand = "node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
